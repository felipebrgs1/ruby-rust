[package]
name = "rbun"
version = "0.1.0"
edition = "2021"
description = "A Bun-like runtime for Ruby: pinned CRuby 3.4 + fork-based fast startup"

[[bin]]
name = "rbun"
path = "src/main.rs"

[profile.release]
lto = true
strip = true
